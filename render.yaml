services:
  - type: web
    name: flask-iris-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: "export PYTHONPATH=$PYTHONPATH:$(pwd)/src && gunicorn --bind 0.0.0.0:$PORT src.app:app"